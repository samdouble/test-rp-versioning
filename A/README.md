# A

Simple Java/Gradle project.

## Build

```bash
./gradlew build
```

If `./gradlew` fails (missing wrapper jar), run once with Gradle installed:

```bash
gradle wrapper
```

Then use `./gradlew build`.

## Run

```bash
./gradlew run
```

## Tests

```bash
./gradlew test
```
