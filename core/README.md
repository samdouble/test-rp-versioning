# core

Shared library used by modules **A** and **B**. Put common utilities, helpers, and shared logic here.

Example: `com.example.VersionInfo` provides `getVersion(Class)` for reading module version from `version.properties`.
